规则("演奏循环动作C")
{
	事件
	{
		持续 - 每名玩家;
		队伍1;
		位置 1;
	}

	条件
	{
		全局变量(Z) == 101;
	}

	动作
	{
		根据条件跳过(比较(数组中的值(全局变量(F), 全局变量(E)), ==, 0), 1);
		设置朝向(此位置的玩家(1, 队伍1), 向量(眼睛位置(此位置的玩家(1, 队伍1)), 数组中的值(全局变量(A), 数组中的值(全局变量(F), 全局变量(E)))), 至地图);
		等待(0.090, 无视条件);
		根据条件跳过(比较(数组中的值(全局变量(F), 全局变量(E)), ==, 0), 1);
		按下按键(此位置的玩家(1, 队伍1), 主要攻击模式);
		等待(0.090, 无视条件);
		设置全局变量(E, 加(全局变量(E), 1));
		根据条件循环(比较(全局变量(E), <=, 999));
		设置全局变量(E, 0);
	}
}

规则("演奏循环动作D")
{
	事件
	{
		持续 - 每名玩家;
		队伍1;
		位置 1;
	}

	条件
	{
		全局变量(Z) == 102;
	}

	动作
	{
		根据条件跳过(比较(数组中的值(全局变量(G), 全局变量(E)), ==, 0), 1);
		设置朝向(此位置的玩家(1, 队伍1), 向量(眼睛位置(此位置的玩家(1, 队伍1)), 数组中的值(全局变量(A), 数组中的值(全局变量(G), 全局变量(E)))), 至地图);
		等待(0.090, 无视条件);
		根据条件跳过(比较(数组中的值(全局变量(G), 全局变量(E)), ==, 0), 1);
		按下按键(此位置的玩家(1, 队伍1), 主要攻击模式);
		等待(0.090, 无视条件);
		设置全局变量(E, 加(全局变量(E), 1));
		根据条件循环(比较(全局变量(E), <=, 584));
		设置全局变量(E, 0);
	}
}