变量
{
	全局:
		3: Music_Score_Right
		4: Beat_Right
}

动作
{
	设置全局变量(Music_Score_Right, 添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(
		添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(
		添加至数组(全局变量(Music_Score_Right), 
		405), 
		503), 504), 505), 503), 
		505), 506), 502), 
		501), 501), 501), 501), 501), 501), 501), 502), 
		503), 505), 503), 503), 
		502), 501), 501), 501), 
		502), 405), 501), 501), 501), 501), 
		502), 503), 503));
	设置全局变量(Beat_Right, 添加至数组(添加至数组(添加至数组(
		添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(
		添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(全局变量(Beat_Right), 
		2), 
		4), 2), 8), 2), 
		4), 2), 10), 
		3), 1), 3), 1), 3), 1), 3), 1), 
		4), 2), 6), 4),
		4), 2), 6), 4), 
		6), 2), 3), 1), 3), 1), 
		2), 4), 24));
}