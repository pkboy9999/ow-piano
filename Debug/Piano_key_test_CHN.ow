规则("DEBUG")
{
	事件
	{
		持续 - 全局;
	}

	条件
	{
		全局变量(Z) == 2;
	}

	动作
	{
		设置全局变量(C, 数组中的值(全局变量(A), 1));
		设置全局变量(D, 数组中的值(全局变量(A), 2));
		设置全局变量(E, 数组中的值(全局变量(A), 3));
		设置全局变量(F, 数组中的值(全局变量(A), 4));
		设置全局变量(G, 数组中的值(全局变量(A), 5));
		设置全局变量(H, 数组中的值(全局变量(A), 6));
		设置全局变量(I, 数组中的值(全局变量(A), 7));
	}
}

规则("DEBUG2")
{
	事件
	{
		持续 - 每名玩家;
		双方;
		全部;
	}

	条件
	{
		按钮被按下(事件玩家, 技能2) == 真;
	}

	动作
	{
		等待(0.016, 无视条件);
		创建效果(所有玩家(所有队伍), 球体, 蓝色, 数组中的值(数组中的值(全局变量(A), 7), 全局变量(T)), 0.010, 无);
		设置全局变量(T, 加(全局变量(T), 1));
		根据条件循环(比较(全局变量(T), <=, 75));
	}
}