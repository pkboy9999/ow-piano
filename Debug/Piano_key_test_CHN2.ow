变量
{
	全局:
		0: Piano_Key
		9: Filter_Right_Key
}

动作
{
	设置全局变量(Filter_Right_Key, 添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(全局变量(Piano_Key), 8), 17), 19),
		21), 23), 25), 27), 29), 31), 33), 35), 41), 43), 45), 47), 49), 51), 53), 55), 57), 59));
}