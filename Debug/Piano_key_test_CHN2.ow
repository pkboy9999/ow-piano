规则("L乐谱录入408A")
{
	事件
	{
		持续 - 全局;
	}

	条件
	{
		全局变量(Z) == 56;
	}

	动作
	{
		设置全局变量(Y, 添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(添加至数组(空数组, 471), 475), 479), 535), 539), 543), 599), 603), 607), 663), 667), 671));
		设置全局变量(Z, 57);
	}
}

规则("L乐谱录入408B")
{
	事件
	{
		持续 - 全局;
	}

	条件
	{
		全局变量(Z) == 57;
	}

	动作
	{
		等待(0.016, 无视条件);
		在索引处设置全局变量(F, 首个(全局变量(Y)), 408);
		修改全局变量(Y, 根据索引从数组中移除, 0);
		根据条件循环(比较(全局变量(Y), !=, 空数组));
		设置全局变量(Z, 58);
	}
}